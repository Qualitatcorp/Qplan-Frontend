/**
* trabajador Module
*
* Termino del sistema
*/
angular.module('trabajador')
.controller('terminoController', ['$location','$timeout','toastr', function($location,$timeout,toastr){
  	// toastr.info('Pronto volvera al comienzo favor acercarse a la mesa', 'Información');
	// $timeout(function() {
	// 	$location.path('/');
	// },10000)
}])